# Does not matter what kind of module you import in the run.py module,
# the print will always be executed (that's a property of __init__.py file)
# print('I will print first')
# Of course, you need to import something at "run.py"







# This archieve is a convention to mark the whole the directory as a package,
# but since Python 3.3 this is not more needed.